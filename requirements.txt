# Minimal runtime dependencies for the MusicScope Streamlit app on
# Streamlit Community Cloud. Heavy notebook/ML/dev dependencies live in
# pyproject.toml optional extras and requirements-dev.txt.

# Core Streamlit stack
streamlit>=1.36.0

# Data and plotting stack (pinned to wheel-available versions)
pandas==2.2.3
numpy==2.1.3
plotly==5.24.1

# Database + config (used by web.etl_helpers.get_engine / read_sql_safe)
sqlalchemy>=2.0.0
pymysql>=1.0.0
python-dotenv>=1.0.0

# Misc runtime utilities
pydantic>=2.7.0
requests>=2.28.0

# Streamlit UI extras imported by streamlit_app.py
streamlit-extras>=0.3.0
streamlit-echarts>=0.4.0
streamlit-option-menu>=0.3.0
streamlit-shadcn-ui>=0.1.0
streamlit-on-Hover-tabs>=1.0.0
