[pytest]
addopts = -q -ra
timeout = 30
timeout_func_only = true
faulthandler_timeout = 60
testpaths = tests
# Limit discovery to the modules that exist in this repo; others target external packages
python_files = test_youtube_channel_etl.py
filterwarnings =
    ignore::DeprecationWarning
